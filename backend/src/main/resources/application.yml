server:
  port: 8080

spring:
  config:
    import: optional:file:../.env[.properties]
  application:
    name: ai-learning-platform
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:learning_db}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DB_USERNAME:admin}
    password: ${DB_PASSWORD:password}
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized

app:
  data-dir: ${APP_DATA_DIR:./data}
  upload-dir: ${APP_DATA_DIR:./data}/uploads
  llm:
    # 提供商优先级列表：按顺序尝试，如 ["zhipu", "deepseek", "qwen"]
    providers: ${LLM_PROVIDERS:zhipu,deepseek,qwen}
    # 指定模型名（可选，不填则使用各提供商的默认模型）
    model: ${LLM_MODEL:}
    zhipu:
      api-base: ${ZHIPU_API_BASE:https://open.bigmodel.cn/api/paas/v4}
      api-key: ${ZHIPU_API_KEY:}
      default-model: ${ZHIPU_DEFAULT_MODEL:glm-4.7-flash}
    deepseek:
      api-base: ${DEEPSEEK_API_BASE:https://api.deepseek.com}
      api-key: ${DEEPSEEK_API_KEY:}
      default-model: ${DEEPSEEK_DEFAULT_MODEL:deepseek-chat}
    qwen:
      api-base: ${QWEN_API_BASE:https://dashscope.aliyuncs.com/compatible-mode/v1}
      api-key: ${QWEN_API_KEY:}
      default-model: ${QWEN_DEFAULT_MODEL:qwen-plus}
  quiz:
    default-count: ${QUIZ_DEFAULT_COUNT:10}
    max-count: ${QUIZ_MAX_COUNT:50}
